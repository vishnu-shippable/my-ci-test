language: none
  
build:
  pre_ci_boot:
    image_name: drydock/c7javall
    image_tag: master
  ci:
    - shipctl jdk set openjdk8
    - java -version
    - shipctl jdk set openjdk9
    - java -version
