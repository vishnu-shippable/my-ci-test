language: java

jdk:
  - oraclejdk8

build:
  ci:
    - java -version
    - javac -version
    - echo "$JAVA_HOME"
    - printenv
    - git clone https://github.com/gradle/gradle-build-scan-quickstart.git
    - cd gradle-build-scan-quickstart
    - ./gradlew build --scan
