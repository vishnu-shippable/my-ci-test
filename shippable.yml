language: none
  
build:
  pre_ci_boot:
    image_name: drydock/u14javall
    image_tag: master
  ci:
    - shipctl jdk set openjdk7
    - java -version
    - shipctl jdk set openjdk8
    - java -version
